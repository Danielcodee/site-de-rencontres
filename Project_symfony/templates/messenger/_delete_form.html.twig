<form method="post" action="{{ path('app_messenger_delete', {'id': messenger.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet élément?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ messenger.id) }}">
    <button class="btn">Supprimer</button>
</form>
